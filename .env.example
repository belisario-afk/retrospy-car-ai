# Copy this file to `.env` and set values. Never commit .env.
# For Create React App, env vars must start with REACT_APP_
REACT_APP_APP_NAME="RetroSpy Car AI"

# Spotify OAuth (PKCE) - Client ID from https://developer.spotify.com/dashboard
REACT_APP_SPOTIFY_CLIENT_ID=927fda6918514f96903e828fcd6bb576

# Redirect URI(s) - must exactly match one of the entries you add on the Spotify dashboard.
# Choose ONE and ensure it matches where the app is hosted (GitHub Pages path).
# If you publish to GitHub Pages under repo `retrospy-car-ai`, your Pages URL looks like:
# https://belisario-afk.github.io/retrospy-car-ai/
# Then your redirect below should be: https://belisario-afk.github.io/retrospy-car-ai/callback
REACT_APP_SPOTIFY_REDIRECT_URI=https://belisario-afk.github.io/sfb/callback
# Optional alternates (uncomment one if you use them and ensure you add it on Spotify Dashboard)
# REACT_APP_SPOTIFY_REDIRECT_URI=https://belisario-afk.github.io/sfbss/callback
# REACT_APP_SPOTIFY_REDIRECT_URI=https://belisario-afk.github.io/sfbss/
# REACT_APP_SPOTIFY_REDIRECT_URI=http://localhost:3000/callback

# App base path for GitHub Pages (used by router). For CRA, we set homepage="." and use relative paths.
# You can optionally set this to your repo path (e.g., /retrospy-car-ai/).
REACT_APP_BASE_PATH=/retrospy-car-ai/

# Optional: set to "true" to enable mock mode without Spotify (local demo sounds)
REACT_APP_MOCK_MODE=false